/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hide the Next.js dev-tools indicator (the floating "N" badge) so it
  // doesn't overlap the sidebar profile avatar in development.
  devIndicators: false,
};
module.exports = nextConfig;
